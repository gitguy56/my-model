# =============================================================================
# LESSON 2: Neural Network Basics
# =============================================================================
# What we'll build:
#   - Load the token numbers from Lesson 1
#   - Turn each token into an "embedding" (a list of floats)
#   - Pass those through a single linear layer
#   - See the model make its first (terrible) prediction
#
# Concepts covered:
#   - torch.nn.Embedding  — lookup table that maps token IDs to float vectors
#   - torch.nn.Linear     — the simplest neural network layer (y = xW + b)
#   - forward pass        — pushing data through the network once
#
# Requires: pip install torch
# =============================================================================

# TODO: coming in Lesson 2
